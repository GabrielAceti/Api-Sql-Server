var config = {
    server: "localhost",
    options: {},
    authentication: {
      type: "default",
      options: {  
        userName: "sa",
        password: "masterkey",
      }
    }
  };

export default config;
